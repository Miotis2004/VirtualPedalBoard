<Window x:Class="PedalBoard.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:PedalBoard.ViewModels;assembly=PedalBoard.ViewModels"
        mc:Ignorable="d"
        Title="Virtual Pedal Board" Height="450" Width="800" Background="#333">
    
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Input Selection -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Input Device (ASIO):" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox ItemsSource="{Binding AvailableDrivers}" 
                      SelectedItem="{Binding SelectedDriver}" 
                      Width="200"/>
        </StackPanel>

        <!-- Pedal Board Area -->
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding Pedals}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#555" CornerRadius="5" Margin="5" Padding="10" Width="150">
                            <StackPanel>
                                <CheckBox IsChecked="{Binding IsEnabled}" Content="{Binding Name}" Foreground="White" FontWeight="Bold" Margin="0,0,0,10"/>
                                
                                <StackPanel Visibility="{Binding HasGain, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <TextBlock Text="Gain" Foreground="#DDD"/>
                                    <Slider Minimum="0" Maximum="5" Value="{Binding Gain}"/>
                                </StackPanel>

                                <StackPanel Visibility="{Binding HasDrive, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <TextBlock Text="Drive" Foreground="#DDD"/>
                                    <Slider Minimum="0" Maximum="1" Value="{Binding Drive}"/>
                                </StackPanel>

                                <StackPanel Visibility="{Binding HasRate, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <TextBlock Text="Rate" Foreground="#DDD"/>
                                    <Slider Minimum="0.1" Maximum="10" Value="{Binding Rate}"/>
                                </StackPanel>

                                <StackPanel Visibility="{Binding HasDepth, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <TextBlock Text="Depth" Foreground="#DDD"/>
                                    <Slider Minimum="0" Maximum="1" Value="{Binding Depth}"/>
                                </StackPanel>

                                <StackPanel Visibility="{Binding HasDecay, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <TextBlock Text="Decay" Foreground="#DDD"/>
                                    <Slider Minimum="0.1" Maximum="0.99" Value="{Binding Decay}"/>
                                </StackPanel>

                                <StackPanel Visibility="{Binding HasMix, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <TextBlock Text="Mix" Foreground="#DDD"/>
                                    <Slider Minimum="0" Maximum="1" Value="{Binding Mix}"/>
                                </StackPanel>

                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
